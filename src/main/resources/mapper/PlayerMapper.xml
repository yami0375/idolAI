<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.moriscustomerservice.mapper.PlayerMapper">
    <select id="selectAvailablePlayers" resultType="com.yami.moriscustomerservice.pojo.Player">
        SELECT * FROM player
        WHERE status = 1  -- 1表示空闲状态
        <if test="level != null">AND level = #{level}</if>
        <if test="gender != null">AND gender = #{gender}</if>
        <if test="minRating != null">AND rating >= #{minRating}</if>
        ORDER BY rating DESC
        <if test="limit != null">LIMIT #{limit}</if>
    </select>

    <select id="selectById" resultType="com.yami.moriscustomerservice.pojo.Player">
        SELECT * FROM player WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE player SET status = #{status}, updated_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="incrementOrderCount">
        UPDATE player SET order_count = order_count + 1, updated_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>