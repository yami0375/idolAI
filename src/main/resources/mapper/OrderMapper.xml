<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.moriscustomerservice.mapper.OrderMapper">
    <insert id="insert" parameterType="com.yami.moriscustomerservice.pojo.Order">
        INSERT INTO `order` (order_no, player_id, customer_id, service_type,
                             amount, start_time, status)
        VALUES (#{orderNo}, #{playerId}, #{customerId}, #{serviceType},
                #{amount}, #{startTime}, #{status})
    </insert>

    <select id="selectByOrderNo" resultType="com.yami.moriscustomerservice.pojo.Order">
        SELECT * FROM `order` WHERE order_no = #{orderNo}
    </select>

    <update id="updateStatus">
        UPDATE `order`
        SET status = #{status}
        <if test="endTime != null">, end_time = #{endTime}</if>
        WHERE order_no = #{orderNo}
    </update>
</mapper>
